/*
1. require JWT
2.  Create Secrete_TOken = "require('crypto').randomBytes(64).toString('hex')"   from command line. At first write on cmd "node".then write this avobe code.

3. copy this result and set it on .env file . 
4. create a JWT api as a POST methode on backend. 


*/